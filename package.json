{
  "name": "cvs-tsk-cert-gen",
  "version": "0.0.1",
  "description": "Certificate generation initiation task",
  "main": "handler.js",
  "scripts": {
    "start": "BRANCH=local BUCKET=local node_modules/serverless/bin/serverless invoke local -f main --path tests/resources/s3-event.json",
    "start:docker": "docker-compose -f docker-compose.yml up -d",
    "build": "node_modules/typescript/bin/tsc --rootDir ./ --outDir .build --sourceMap false && npm run build:copy",
    "build:dev": "node_modules/typescript/bin/tsc && npm run build:copy",
    "build:copy": "find src -type f \\( -name \"*.yml\" -or -name \"*.json\" \\) | cpio -pdm .build && find tests -type f \\( -name \"*.yml\" -or -name \"*.json\" -or -name \"*.base64\" \\)  | cpio -pdm .build",
    "build:docker": "docker-compose -f docker-compose.yml up -d --build",
    "test": "BUCKET=local BRANCH=local node_modules/.bin/mocha .build/tests/**/*.unitTest.js",
    "lint": "node_modules/tslint/bin/tslint --project tsconfig.json -q",
    "prepush": "npm test && npm run lint",
    "sonar-scanner": "npm run lint && npm run coverage && sonar-scanner",
    "coverage": "node_modules/.bin/nyc mocha tests/unit/*.unitTest.ts && node_modules/.bin/nyc report --reporter=lcov"
  },
  "nyc": {
    "include": [
      "src/**/*.ts",
      "src/**/*.tsx"
    ],
    "extension": [
      ".ts",
      ".tsx"
    ],
    "require": [
      "ts-node/register"
    ],
    "reporter": [
      "text-summary",
      "html"
    ],
    "sourceMap": true,
    "instrument": true
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-lambda": "^0.1.2",
    "aws-sdk": "^2.416.0",
    "node-yaml": "^3.1.1",
    "notifications-node-client": "^4.6.0",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.21",
    "@types/chai": "^4.1.7",
    "@types/lambda-tester": "^3.5.1",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.12.30",
    "chai": "^4.2.0",
    "lambda-tester": "latest",
    "mocha": "^5.2.0",
    "nyc": "^13.3.0",
    "serverless": "^1.36.3",
    "serverless-plugin-typescript": "^1.1.5",
    "ts-node-register": "^1.0.0",
    "tslint": "^5.13.1",
    "typescript": "^3.3.3333"
  }
}
